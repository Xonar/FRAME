#version 140

//Input
in vec2 pos;
in vec2 tex;
in int norm;

//Output
out vec2 vstex;
flat out int vschar;

uniform Matrices
{
  uniform mat4 Ortho;
};

//Main
void main()
{
  vstex = tex;
  vschar = norm;

  gl_Position = Ortho * vec4(pos, 0, 1);
}
