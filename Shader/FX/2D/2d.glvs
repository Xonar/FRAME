#version 140

//Input
in vec2 pos;
in vec2 tex;
in vec3 col;

//Output
out vec2 vstex;
out vec3 vscol;

//uniform mat4 TextOrthoMatrix;

//Main
void main()
{
  vstex = tex;
  vscol = col;
  gl_Position = /*TextOrthoMatrix * */ vec4(pos, 0, 1);
}
