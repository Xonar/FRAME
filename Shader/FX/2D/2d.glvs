#version 140

//Input
in vec2 pos;
in vec2 tex;
in vec3 col;

//Output
vec2 vstex;
vec3 vscol;

uniform mat4 TextOrthoMatrix;

//Main
void main()
{
  vstex = tex;
  vscol = col;
  gl_Position = TextOrthoMatrix * vec4(pos,1.f,1.f);
}
